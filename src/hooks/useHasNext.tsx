// import { useEffect, useState } from "react";

// export const useHasNext = (feedLength: number) => {
//     const [count, setCount] = useState(5);
//     const [hasMoreItems, setHasMoreItems] = useState(count < feedLength);

//     function nextPage() {
//         let newCount = count + 10;
//         let hasMoreItems = newCount < feed.length;
//         setCount(hasMoreItems ? newCount : feed.length);
//         setHasMoreItems(hasMoreItems)
//     }
// }
